version: 2.1
jobs:
  build:
    working_directory: /go/src/github.com/argoproj/argo-rollouts
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run:
          command: dep ensure
      - run:
          command: make test
workflows:
  version: 2
  workflow:
    jobs:
      - build
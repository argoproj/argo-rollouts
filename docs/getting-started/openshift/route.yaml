apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: deployment-example
  name: stable-service
  namespace: argo-rollouts
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: stable-service
    weight: 100
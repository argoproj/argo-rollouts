name: Docker

on:
  push:
    branches:
      - master
      - release-*
    tags:
      - v*.*.*

  # Run tests for any PRs.
  pull_request:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Docker
      uses: ./.github/actions/docker

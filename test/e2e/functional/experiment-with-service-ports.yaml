apiVersion: argoproj.io/v1alpha1
kind: Experiment
metadata:
  name: experiment-with-service-ports
spec:
  duration: 10s
  scaleDownDelaySeconds: 5
  # List of pod template specs to run in the experiment as ReplicaSets
  templates:
    - name: test
      replicas: 1
      service:
        name: test-service-with-ports
        ports:
          - port: 80
            targetPort: 8080
          - port: 81
            targetPort: 8081
      selector:
        matchLabels:
          app: experiment-with-service-ports
      template:
        metadata:
          labels:
            app: experiment-with-service-ports
        spec:
          containers:
            - name: experiment-with-service-ports
              image: nginx:1.19-alpine
              resources:
                requests:
                  memory: 16Mi
                  cpu: 1m
              ports:
                 - name: testport1
                   protocol: TCP
                   containerPort: 8080
                 - name: testport2
                   protocol: TCP
                   containerPort: 8081

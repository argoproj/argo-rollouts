apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: rollouts-dashboard
    app.kubernetes.io/name: rollouts-dashboard
    app.kubernetes.io/part-of: argo-rollouts
  name: argo-rollouts-dashboard-service
spec:
  selector:
    app.kubernetes.io/name: rollouts-dashboard
  ports:
    - port: 3100
      protocol: TCP
      targetPort: 3100
